server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chapter_97
    username: root
    password: 12341234
    driver-class-name: com.mysql.cj.jdbc.Driver

springdoc:
  api-docs:
    path: /api-docs  # 기본 API 문서 경로는 /v3/api-docs, 필요에 따라 변경 가능
  swagger-ui:
    path: /swagger-ui  # 기본 Swagger UI 경로는 /swagger-ui.html, 필요에 따라 변경 가능
logging:
  config: classpath:logback-spring-local.xml
